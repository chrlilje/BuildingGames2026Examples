# Copilot Instructions

- Never use `var`.
- Always use `const` by default.
- Use `let` only when reassignment is required.
- Keep code beginner-friendly and avoid arrow functions unless explicitly requested.
- Use `function` declarations for all functions to maintain consistency and readability for beginners.